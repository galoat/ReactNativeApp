spring:
  cloud:
    config:
      uri: http://localhost:8888
  application:
    name: feedinfo-service
management:
  endpoints:
    web:
      exposure:
        include: "*"
---
spring:
  profiles: docker
  cloud:
    config:
      uri: http://config-server:8888
