- hosts: all
  become: true
  remote_user: root
  roles:
  tasks:
     - file: path=/app/docker state=directory
     - copy:
           src: ../../docker-compose.yml
           dest: /app/docker
     - copy:
          src: ../data/dump.sql
          dest: /app/docker/dautodeploy/data/
     -  name: execute docker commpose
        shell: docker-compose up -d
        args:
            chdir: /app/docker
